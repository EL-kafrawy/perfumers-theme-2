<section class="py-24 bg-pearl-beige">
  <h3 class="font-serif text-3xl text-center mb-10 italic tracking-tight text-4xl">{{ section.settings.title }}</h3>
  <div class="flex flex-col gap-4 px-4">
    {%- for block in section.blocks -%}
      {%- if forloop.first -%}
        <div class="relative h-48 w-full overflow-hidden group" {{ block.shopify_attributes }}>
          {%- if block.settings.image != blank -%}
            <img class="w-full h-full object-cover grayscale-[0.4] group-hover:scale-105 transition-transform duration-1000" src="{{ block.settings.image | image_url: width: 1400 }}" alt="{{ block.settings.title | escape }}">
          {%- else -%}
            <div class="w-full h-full" style="background: #d8cfc9;"></div>
          {%- endif -%}
          <div class="absolute inset-0 bg-black/30 flex items-center justify-center">
            <h4 class="text-white font-serif text-3xl italic">{{ block.settings.title }}</h4>
          </div>
        </div>
        {%- if section.blocks.size > 1 -%}<div class="grid grid-cols-2 gap-4">{%- endif -%}
      {%- else -%}
        <div class="relative h-48 overflow-hidden group" {{ block.shopify_attributes }}>
          {%- if block.settings.image != blank -%}
            <img class="w-full h-full object-cover grayscale-[0.4] group-hover:scale-105 transition-transform duration-1000" src="{{ block.settings.image | image_url: width: 800 }}" alt="{{ block.settings.title | escape }}">
          {%- else -%}
            <div class="w-full h-full" style="background: #d8cfc9;"></div>
          {%- endif -%}
          <div class="absolute inset-0 bg-black/20 flex items-center justify-center text-center px-4">
            <h4 class="text-white font-serif text-2xl italic">{{ block.settings.title }}</h4>
          </div>
        </div>
      {%- endif -%}
    {%- endfor -%}
    {%- if section.blocks.size > 1 -%}</div>{%- endif -%}
  </div>
</section>

<style>
  .py-24 { padding-top: 6rem; padding-bottom: 6rem; }
  .bg-pearl-beige { background-color: #E7DDD4; }
  .font-serif { font-family: 'Cormorant Garamond', serif; }
  .text-3xl { font-size: 1.875rem; }
  .text-center { text-align: center; }
  .mb-10 { margin-bottom: 2.5rem; }
  .italic { font-style: italic; }
  .tracking-tight { letter-spacing: -0.025em; }
  .text-4xl { font-size: 2.25rem; }
  .flex { display: flex; }
  .flex-col { flex-direction: column; }
  .gap-4 { gap: 1rem; }
  .px-4 { padding-left: 1rem; padding-right: 1rem; }
  .relative { position: relative; }
  .h-48 { height: 12rem; }
  .w-full { width: 100%; }
  .overflow-hidden { overflow: hidden; }
  .group { }
  .w-full { width: 100%; }
  .h-full { height: 100%; }
  .object-cover { object-fit: cover; }
  .grayscale-\[0\.4\] { filter: grayscale(0.4); }
  .group:hover .group-hover\:scale-105 { transform: scale(1.05); }
  .transition-transform { transition-property: transform; }
  .duration-1000 { transition-duration: 1000ms; }
  .absolute { position: absolute; }
  .inset-0 { inset: 0; }
  .bg-black\/30 { background-color: rgba(0,0,0,0.3); }
  .bg-black\/20 { background-color: rgba(0,0,0,0.2); }
  .items-center { align-items: center; }
  .justify-center { justify-content: center; }
  .text-white { color: #fff; }
  .text-2xl { font-size: 1.5rem; }
  .text-center { text-align: center; }
  .px-4 { padding-left: 1rem; padding-right: 1rem; }
  .grid { display: grid; }
  .grid-cols-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
</style>

{% schema %}
{
  "name": "Shop by Occasion",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section title",
      "default": "Choose Your Mood"
    }
  ],
  "blocks": [
    {
      "type": "occasion",
      "name": "Occasion",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Background image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Occasion title"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Collection link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Shop by Occasion",
      "blocks": [
        { "type": "occasion", "settings": { "title": "Nocturnal Elegance" } },
        { "type": "occasion", "settings": { "title": "Morning Dew" } },
        { "type": "occasion", "settings": { "title": "Stately Library" } }
      ]
    }
  ]
}
{% endschema %}
